# ── Environment ───────────────────────────────────────
.env
.env.local
.env.*.local

# ── uv (will be REMOVED after uv init — lock file gets committed) ──
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# ── FastF1 cache (can be hundreds of MBs) ─────────────
fastf1_cache/

# ── ML artifacts ──────────────────────────────────────
mlruns/
ml_models/
*.pkl
*.joblib
*.parquet

# ── Logs / runtime artifacts ──────────────────────────
*.log

# ── Node / Next.js ────────────────────────────────────
node_modules/
.next/
.pnpm-store/

# ── OS ────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── IDE (share extensions.json but not personal settings) ──
.vscode/settings.json
.idea/

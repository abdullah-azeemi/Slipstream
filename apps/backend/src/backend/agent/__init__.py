"""
    Slipstream Agent package

    It is a deterministic read-only data tool behind the agent orchestration.
    Rule: Tools never write on the database. The SQL lives in tools, not in the LLM output.

"""

[tool.uv.workspace]
members = [
    "apps/backend",
    "apps/ml",
    "packages/ingestion",
    "packages/stream",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "mypy>=1.11",
]
